pipeline {
    agent any
    stages {
         stage ('React.js Build')
         {
             steps {
                 sh 'npm run build'
                 echo 'UI React.js Project Build Successfully!!!'
             }
         }
    }
}